"use client";

/** Thin chrome around @nivra/ui charts so every calculator shares Goal SIP frame language. */
export { ChartFrame, ChartTooltipCard, chartAxisTick, chartGrid } from "./charts/chart-frame";
